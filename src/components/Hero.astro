---
import { Image } from "astro:assets";
import schneider from "../assets/schneider.png";
import rakete from "../assets/Rakete.png";
import design from "../assets/Softwaredesign.png";
import digitalisierung from "../assets/Digitalisierung.png";
import entwicklung from "../assets/Produktentwicklung.png";
import businessAnalyse from "../assets/Business-Analyse.png";
import projektmanagement from "../assets/Projektmanagement.png";

export interface Slide {
    id: string;
    heading: string;
    subtext: string;
    ctaLabel: string;
    ctaHref: string;
    bgGradient: string;
    image: ImageMetadata;
    imageGradient?: string;
}

const slides: Slide[] = [
    {
        id: "einstieg",
        heading:
            "Sie möchten ein Projekt realisieren, sind jedoch unsicher, wie Sie vorgehen sollen?",
        subtext:
            "Mit uns bekommen Sie einen verlässlichen Partner für Ihre maßgeschneiderte Softwarelösung von A bis Z.",
        ctaLabel: "Jetzt kontaktieren",
        ctaHref: "/kontakt/",
        bgGradient: "linear-gradient(300deg, #fefdcd, #a3e6ff)",
        image: schneider,
        imageGradient: "linear-gradient(#22c1c3, #c98b32)",
    },
    {
        id: "startup",
        heading: "Erfolgreiche Softwareentwicklung für Startups",
        subtext:
            "Du hast eine geniale Idee für ein Startup, einen Investor an der Hand und bist bereit durchzustarten.",
        ctaLabel: "Jetzt durchstarten",
        ctaHref: "/leistungen/startups/",
        bgGradient: "linear-gradient(300deg, #fcdabd, #cbdef4)",
        image: rakete,
        imageGradient: "linear-gradient(#c99e65, #c98b32)",
    },
    {
        id: "produktentwicklung",
        heading: "Partner für maßgeschneiderte Softwarelösungen",
        subtext:
            "Von der Innovation eines Startups bis zur Weiterentwicklung etablierter Projekte, bringen wir Ihre Visionen zum Leben. Leidenschaftlich. Kreativ. Zuverlässig.",
        ctaLabel: "Ihre Vision, unsere Passion",
        ctaHref: "/leistungen/produktentwicklung/",
        bgGradient: "linear-gradient(300deg, #fbe0fc, #bbf9e7)",
        image: entwicklung,
        imageGradient: "linear-gradient(#96edf4, #f15fa2)",
    },
    {
        id: "softwaredesign",
        heading: "Die Kunst des Softwaredesigns",
        subtext:
            "Jedes Projekt ist einzigartig, und wir setzen unsere Leidenschaft und Expertise ein, um Software zu gestalten, die perfekt zu Ihren Wünschen und Anforderungen passt.",
        ctaLabel: "Details erkunden",
        ctaHref: "/leistungen/softwaredesign/",
        bgGradient: "linear-gradient(300deg, #c5daf9, #dbffea)",
        image: design,
        imageGradient: "linear-gradient(#96f4e9, #5f92f1)",
    },
    {
        id: "digitalisierung",
        heading: "Digitalisierung",
        subtext:
            "Nutzen Sie die Chancen der Digitalisierung und bleiben Sie wettbewerbsfähig in einer sich ständig verändernden Geschäftswelt. Wir helfen Ihnen dabei.",
        ctaLabel: "Entdecken Sie ihre digitale Zukunft",
        ctaHref: "/leistungen/digitalisierung/",
        bgGradient: "linear-gradient(300deg, #fcdbcf, #d4f4e9)",
        image: digitalisierung,
        imageGradient: "linear-gradient(#f4e996, #f1c25f)",
    },
    {
        id: "business-analyse",
        heading: "Business Analyse",
        subtext:
            "Die Business-Analyse ist ein entscheidender Prozess für Softwareunternehmen. Sie ermöglicht es, die Effizienz und Effektivität der Geschäftsabläufe zu optimieren.",
        ctaLabel: "Entdecken Sie Ihr Potenzial",
        ctaHref: "/leistungen/business-analyse/",
        bgGradient: "linear-gradient(300deg, #e2f7ff, #defcbd)",
        image: businessAnalyse,
        imageGradient: "linear-gradient(#96f4d7, #5ff16a)",
    },
    {
        id: "projektmanagement",
        heading: "Projektmanagement",
        subtext:
            "Die Entwicklung großartiger Software erfordert nicht nur technisches Know-how, sondern auch effektives Projektmanagement. Bei depero sind wir stolz auf unsere Fähigkeit, Projekte reibungslos und termingerecht abzuschließen.",
        ctaLabel: "Gemeinsam zum Erfolg",
        ctaHref: "/leistungen/projektmanagement/",
        bgGradient: "linear-gradient(300deg, #9fecfc, #d2ddef)",
        image: projektmanagement,
        imageGradient: "linear-gradient(#8987ff, #d79dea)",
    },
];

const { activeSlide = 0 } = Astro.props as { activeSlide?: number };
---

<section class="hero" aria-label="Hero Slider">
    {
        slides.map((slide, i) => (
            <div
                class="hero-slide"
                class:list={{ "hero-slide--active": i === activeSlide }}
                style={{ background: slide.bgGradient }}
                aria-hidden={i !== activeSlide}
            >
                <div class="container hero-content">
                    <div class="hero-text">
                        <h1 class="hero-heading">{slide.heading}</h1>
                        <p class="hero-subtext">{slide.subtext}</p>
                        <a href={slide.ctaHref} class="hero-cta">
                            {slide.ctaLabel}
                        </a>
                    </div>
                    {slide.image && (
                        <div class="hero-image-wrap">
                            <div
                                class="hero-image-ring"
                                style={{ background: slide.imageGradient }}
                            >
                                <Image
                                    src={slide.image}
                                    alt=""
                                    class="hero-image"
                                    loading={i === 0 ? "eager" : "lazy"}
                                />
                            </div>
                        </div>
                    )}
                </div>
            </div>
        ))
    }
    <div class="hero-controls" role="tablist" aria-label="Slide Navigation">
        {
            slides.map((slide, i) => (
                <button
                    class="hero-dot"
                    class:list={{ "hero-dot--active": i === activeSlide }}
                    role="tab"
                    aria-selected={i === activeSlide}
                    aria-label={`Slide ${i + 1}: ${slide.heading.substring(0, 40)}`}
                    data-slide={i}
                >
                    <span class="sr-only">Slide {i + 1}</span>
                </button>
            ))
        }
    </div>
</section>

<script>
    class HeroSlider {
        current = 0;
        slides;
        dots;
        interval;

        constructor() {
            this.slides = document.querySelectorAll(".hero-slide");
            this.dots = document.querySelectorAll(".hero-dot");
            if (this.slides.length <= 1) return;

            this.dots.forEach((dot) => {
                dot.addEventListener("click", () => {
                    const idx = parseInt(dot.dataset.slide ?? "0");
                    this.goTo(idx);
                    this.resetInterval();
                });
            });

            this.startInterval();
        }

        goTo(idx) {
            this.slides.forEach((s, i) => {
                s.classList.toggle("hero-slide--active", i === idx);
                s.setAttribute("aria-hidden", i !== idx ? "true" : "false");
            });
            this.dots.forEach((d, i) => {
                d.classList.toggle("hero-dot--active", i === idx);
                d.setAttribute("aria-selected", i === idx ? "true" : "false");
            });
            this.current = idx;
        }

        startInterval() {
            this.interval = setInterval(() => {
                this.goTo((this.current + 1) % this.slides.length);
            }, 6000);
        }

        resetInterval() {
            clearInterval(this.interval);
            this.startInterval();
        }
    }

    document.addEventListener("astro:page-load", () => {
        new HeroSlider();
    });
</script>

<style>
    .hero {
        position: relative;
        overflow: hidden;
        margin-top: 70px;
    }

    .hero-slide {
        display: none;
        min-height: 600px;
        padding: 100px 0;
        align-items: center;
    }

    .hero-slide--active {
        display: flex;
    }

    .hero-content {
        display: flex;
        align-items: center;
        gap: 60px;
        width: 100%;
    }

    .hero-text {
        flex: 1;
        min-width: 0;
        animation: fadeInUp 0.8s ease-out;
    }

    .hero-heading {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 20px;
        color: var(--dark);
        animation: fadeInUp 0.8s ease-out 0.1s both;
    }

    .hero-subtext {
        font-size: 1.1rem;
        line-height: 1.7;
        color: #444;
        margin-bottom: 32px;
        max-width: 600px;
        animation: fadeInUp 0.8s ease-out 0.2s both;
    }

    .hero-cta {
        display: inline-block;
        padding: 14px 36px;
        border: 2px solid var(--dark);
        border-radius: 50px;
        font-family: var(--font-heading);
        font-size: 1rem;
        font-weight: 700;
        color: var(--dark);
        background: transparent;
        transition:
            background 0.3s,
            color 0.3s;
        animation: fadeInUp 0.8s ease-out 0.3s both;
    }

    .hero-cta:hover {
        background: var(--dark);
        color: #fff;
    }

    .hero-image-wrap {
        flex-shrink: 0;
        width: 520px;
        height: 520px;
        animation: scaleIn 0.8s cubic-bezier(0.34, 1.56, 0.64, 1) 0.15s both;
    }

    .hero-image-ring {
        width: 100%;
        height: 100%;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        animation: float 6s ease-in-out infinite;
    }

    .hero-image {
        width: 70%;
        height: 70%;
        object-fit: contain;
        filter: drop-shadow(0 4px 12px rgba(0, 0, 0, 0.1));
    }

    .hero-controls {
        position: absolute;
        bottom: 30px;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        gap: 10px;
    }

    .hero-dot {
        width: 12px;
        height: 12px;
        border-radius: 50%;
        border: 2px solid var(--dark);
        background: transparent;
        cursor: pointer;
        transition: background 0.3s;
    }

    .hero-dot--active {
        background: var(--dark);
    }

    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes scaleIn {
        from {
            opacity: 0;
            transform: scale(0);
        }
        to {
            opacity: 1;
            transform: scale(1);
        }
    }

    @keyframes float {
        0%,
        100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-10px);
        }
    }

    @media (max-width: 970px) {
        .hero-slide {
            min-height: auto;
            padding: 60px 0 80px;
        }

        .hero-content {
            flex-direction: column;
            text-align: center;
            gap: 40px;
        }

        .hero-heading {
            font-size: 1.8rem;
        }

        .hero-subtext {
            max-width: 100%;
        }

        .hero-image-wrap {
            width: 240px;
            height: 240px;
        }
    }

    @media (max-width: 750px) {
        .hero-slide {
            padding: 40px 0 70px;
        }

        .hero-heading {
            font-size: 1.4rem;
        }

        .hero-subtext {
            font-size: 1rem;
        }

        .hero-image-wrap {
            width: 180px;
            height: 180px;
        }
    }
</style>
